# URL del backend de conversion PDF (Render)
# En desarrollo local se usa el proxy de Vite, dejar vacio o con localhost
# En Vercel, configurar esta variable con la URL de Render
VITE_API_URL=http://localhost:3001/api
